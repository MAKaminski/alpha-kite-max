# Minimal requirements for Lambda deployment
# Only essential runtime dependencies - optimized for size

# Core API clients
schwab-py==1.5.1
supabase==2.11.0

# Configuration and validation
pydantic==2.12.2
pydantic-settings==2.11.0
python-dotenv==1.1.1

# Logging
structlog==25.4.0

# Date/time utilities
python-dateutil==2.9.0
pytz==2025.2

# AWS SDK (already available in Lambda runtime)
boto3==1.40.53

# Note: Excluded heavy packages like pandas, numpy to keep package under 5MB
# These are not needed for the core Lambda functionality
